{% extends '::base.html.twig' %}

{% block page_title %}PersonasHasNodos{% endblock %}
{% block page_subtitle %}Listado{% endblock %}

{% block botones_accion %}
    <a href="{{ path('personashasnodos_new') }}" title="Alta" class="btn btn-success"><i class="fa fa-plus"></i></a>
{% endblock %}

{% block page_content %}
<div class="box">
	<div class="box-body">
	    <table id="tabla" class="table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Persona</th>
                <th>Nodo</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('personashasnodos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td><a href="{{ path('personas_show', { 'id': entity.personas.id }) }}">{{ entity.personas.nombre }} {{ entity.personas.apellidos }}</a></td>
                <td><a href="{{ path('nodos_show', { 'id': entity.nodos.id }) }}">{{ entity.nodos.nombre }}</a></td>
                <td class="text-right">
					                    	<a href="{{ path('personashasnodos_show', { 'id': entity.id }) }}" title="Detalle" class="btn btn-sm btn-info"><i class="fa fa-search"></i></a>
                    
						                    <a href="{{ path('personashasnodos_edit', { 'id': entity.id }) }}" title="Editar" class="btn btn-sm btn-warning"><i class="fa fa-pencil"></i></a>
	                                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

	</div>
</div>
{% endblock %}

{% block javascripts_inline %}
    <script type="text/javascript">
      $(function () {
      	$('a').tooltip();      
        $('#tabla').DataTable({
          "paging": true,
          "lengthChange": false,
          "searching": false,
          "ordering": true,
          "info": true,
          "autoWidth": false
        });
      });
    </script>
{% endblock %}